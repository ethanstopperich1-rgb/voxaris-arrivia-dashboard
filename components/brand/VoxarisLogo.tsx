export function VoxarisLogo({ className }: { className?: string }) {
  return (
    <span className={`font-semibold tracking-widest text-cyan-400 ${className ?? ""}`}>
      VOXARIS
    </span>
  );
}
